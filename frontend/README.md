# Frontend

Vite + React + Tailwind. Talks to the FastAPI backend on `:8000`.

## Run

```bash
cd frontend
npm install
npm run dev
```

Open http://localhost:5173. The page should show the backend `/health` response.
